let g:ctrlp_user_command = 'cd %s && ((test -e ./.git && git ls-files -o --exclude-standard --cached) || find * -type f)'
let g:ctrlp_root_markers = ['.ctrlp']

" no highlight that remains after incremental search
set nohlsearch

" command prompt with ;
map ; :
